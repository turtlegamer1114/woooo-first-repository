public class Main {
    public static void main(String[] args) {
        Car mydino = new dino ("Dinosaur", "Trex");
        
        mydino.size(30);        // sets size to 30
        mydino.speed(10);      // sets speed to 10
        mydino.speed(25);      // changes speed to 25
        mydino.strength(50);
    }
}